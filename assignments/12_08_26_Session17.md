# Session 17 Assignment: Exception Handling Part 2

**Student Name:** Mahissh  
**Subject:** CBSE Grade 12 Computer Science (Code 083)  
**Max Marks:** 30  

---

### Tier 1: Direct Application (10 Marks)

**Q1. Fill in the Blanks: (4 Marks)**
a) The keyword used to programmatically trigger an exception in Python is `_____________`.
b) An `assert` statement raises a(n) `_____________Error` if the specified condition is False.
c) To define a custom exception class, the class must inherit from Python's base `_____________` class.
d) Running a Python program with the option `python -O script.py` disables all `_____________` statements.

**Q2. Refactoring to Exceptions: (6 Marks)**
Look at the following code snippet that checks the validity of a password:
```python
def check_password(pwd):
    if len(pwd) < 8:
        print("Error: Password too short!")
        return False
    else:
        print("Password accepted.")
        return True
```
Refactor this function to manually raise a `ValueError` with the message `"Password must be at least 8 characters!"` instead of printing an error message. Then write a `try-except` block calling the function with `"secret"` to show how the error is handled.

---

### Tier 2: CBSE Exam-Style (10 Marks)

**Q3. Predict the Output: (5 Marks)**
Trace the execution and write the exact output of the following Python program:
```python
def division(a, b):
    try:
        print("Inner Start")
        res = a / b
        print("Inner Success")
        return res
    except TypeError:
        print("Inner TypeError Caught")
        return None
    finally:
        print("Inner Clean Up")

def execute():
    try:
        print("Outer Start")
        ans = division(10, 0)
        print("Outer Success, Result =", ans)
    except ZeroDivisionError:
        print("Outer ZeroDivisionError Caught")
    finally:
        print("Outer Clean Up")

execute()
```

**Q4. Difference Chart: (5 Marks)**
Explain the key differences between `raise` and `assert` statements in Python. Mention at least three technical differences.

---

### Tier 3: Logic & Edge Cases (10 Marks)

**Q5. Designing Custom Exceptions: (5 Marks)**
Write a Python program that:
1. Defines a custom exception class named `NegativeSalaryError`.
2. Implements a class `Employee` with a constructor (`__init__`) that accepts a `salary` attribute. If `salary` is negative, raise the `NegativeSalaryError` with the message `"Salary cannot be negative: <value>"`.
3. Implements code inside a `try-except` block to test a negative salary value and print the exception message.

**Q6. Assertion Flag Override Trap: (5 Marks)**
Look at the following function designed to validate user bank transaction values:
```python
def process_withdrawal(balance, amount):
    assert amount > 0, "Withdrawal amount must be positive!"
    assert amount <= balance, "Insufficient funds!"
    balance -= amount
    return balance
```
Explain:
1. What will happen if this code is executed under standard conditions with `process_withdrawal(500, -100)`?
2. What severe security bug will occur if the user runs the Python program in optimized mode (`python3 -O main.py`) and passes the same inputs? How does this impact the balance variable?
