# Assignment 7: Dictionaries & Function Basics

## Part 1: Dictionary Drills
1.  **Phonebook:** Create a dictionary named `phonebook` with 3 names and their phone numbers.
2.  **Safe Access:** Attempt to access the phone number of a name "Stranger" (who is not in the dictionary) using `.get()` so that it returns "Not Found" instead of an error.
3.  **The Overwriter:** What is the output of the following? Why?
    ```python
    data = {"score": 10, "score": 20}
    print(data["score"])
    ```
4.  **Key Check:** Which of these dictionary definitions will fail? Why?
    - `d = {101: "Ram"}`
    - `d = {"Ram": 101}`
    - `d = {[1, 2]: "Numbers"}`
    - `d = {(1, 2): "Numbers"}`

---

## Part 2: Functions & Flow
1.  **Define and Call:** Write a function called `print_square(n)` that takes a number as a parameter and prints its square. Call the function with the number `7`.
2.  **Tracing the Path:** Write down the output of this code **WITHOUT** running it. Trace it carefully!
    ```python
    def step1():
        print("B")

    def step2():
        print("D")
        step1()

    print("A")
    step2()
    print("C")
    ```
3.  **Identify:** In the code `calculate_area(5, 10)`, what are the numbers 5 and 10 called? (Parameters or Arguments?)

---

## Part 3: Intuition Challenge
**The Mystery Output:** What does this code print? Why doesn't the value of `a` change inside the function? (Just try to think about it, we will discuss in next class!)
```python
a = 5
def change_val():
    a = 10

change_val()
print(a)
```
