# Session 19 Assignment: Data Structures — Stack: Part 2

**Student Name:** Mahissh  
**Subject:** CBSE Grade 12 Computer Science (Code 083)  
**Max Marks:** 30  

---

### Tier 1: Direct Application (10 Marks)

**Q1. Fill in the Blanks: (4 Marks)**
a) If stack elements are tuples, accessing the second field of the top element is written as `stack[-1][____]`.
b) To empty a stack, we should use a `________` loop instead of a `for` loop to prevent index skipping.
c) In a stack display function, the element printed first represents the `________` of the stack.
d) In a negative-step range `range(start, -1, -1)`, the last index included in the loop is `________`.

**Q2. Data Extraction Completion: (6 Marks)**
Given the following dataset representing student exams:
`exams = [{"name": "Aanya", "score": 88}, {"name": "Kabir", "score": 62}]`
Write Python code snippets to:
1. Access Kabir's score using indices. (3 Marks)
2. Complete the code below to push only the names of students with score $\ge 75$ onto `stk`: (3 Marks)
   ```python
   stk = []
   for s in exams:
       if _____________________:
           _____________________
   ```

---

### Tier 2: CBSE Exam-Style (10 Marks)

**Q3. Inventory Stock Filter Stack: (5 Marks)**
A retail shop represents inventory item records as dictionaries with keys `"item"`, `"price"`, and `"qty"`.
`inventory = [{"item": "Notebook", "price": 45, "qty": 15}, {"item": "Pen", "price": 10, "qty": 85}, {"item": "Marker", "price": 30, "qty": 8}]`
Write two functions in Python:
1. `push_low_stock(inv_list)`: Pushes the **item name** (string) of products that have `qty < 20` onto a global stack `low_stock_stk`.
2. `pop_all()`: Pops and prints the names from `low_stock_stk` in LIFO order. If the stack is empty, display `"Stack Empty"`.

**Q4. Employee Dept Grouping Stack: (5 Marks)**
An organization stores employee data as tuples `(emp_id, emp_name, dept, salary)`.
`employees = [(1, "Alok", "HR", 45000), (2, "Bina", "IT", 60000), (3, "Charu", "Sales", 35000), (4, "David", "IT", 70000)]`
Write functions in Python:
1. `push_it_dept(emp_records, stk)`: Pushes the **entire employee tuple record** onto the stack `stk` if they work in the `"IT"` department.
2. `pop_it_employees(stk)`: Pops and prints the name and salary of the popped employee in this format: `Name: <name>, Salary: <salary>`. Display `"Underflow"` if `stk` is empty.

---

### Tier 3: Logic & Edge Cases (10 Marks)

**Q5. Vertical Stack Display with Pointer: (5 Marks)**
Write a Python function `display_stack(stk)` that displays the stack contents vertically from Top to Bottom. The top element must have the suffix `" <-- TOP"` printed next to it. If the stack is empty, print `"No elements in stack"`.
*Example for stk `["Pen", "Pencil", "Eraser"]`:*
```
Eraser <-- TOP
Pencil
Pen
```

**Q6. Tracing the Pop Trap Output: (5 Marks)**
Look at the following python script:
```python
packages = ["PKG1", "PKG2", "PKG3", "PKG4"]
stk = []
for pkg in packages:
    stk.append(pkg)

print("Popping elements:")
for item in stk:
    print("Popped:", stk.pop())

print("Final Stack State:", stk)
```
1. Write the exact output generated by this script. (3 Marks)
2. Explain why the loop terminated before the stack was empty. (2 Marks)
