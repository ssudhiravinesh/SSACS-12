# Session 15 Assignment: CSV File Handling

**Student Name:** Mahissh  
**Subject:** CBSE Grade 12 Computer Science (Code 083)  
**Max Marks:** 30  

---

### Tier 1: Direct Application (10 Marks)

**Q1. Fill in the Blanks: (4 Marks)**
a) The Python module used to read and write CSV files is `_____________`.
b) When opening a CSV file for writing, we pass `newline=''` to `open()` to prevent extra `_____________` lines.
c) To write a single row consisting of a list of items to a CSV file, we call `writer._____________()`.
d) To write a 2D list containing multiple records at once to a CSV file, we call `writer._____________()`.

**Q2. Code Analysis & Error Spotting: (6 Marks)**
Given the following Python snippet:
```python
import csv
records = [["Roll", "Name", "Marks"], [1, "Mahissh", 95], [2, "Aditya", 88]]
with open("report.csv", "w") as f:
    w = csv.writer(f)
    w.writerow(records)
```
1. Identify the structural error when calling `w.writerow(records)`.
2. Describe what output will actually be written to `report.csv` as a result of this mistake.
3. Write the corrected code statement.

---

### Tier 2: CBSE Exam-Style (10 Marks)

**Q3. User Credential Password Finder: (5 Marks)**
Write a Python function `search_user_password(target_user)` that reads a CSV file `user_credentials.csv` containing columns `user_id` and `password`. The function should search for `target_user`. If found, print `"Password for <target_user> is: <password>"`. If not found, print `"User ID not found"`.

**Q4. Item Inventory CSV Creator: (5 Marks)**
Write a Python function `create_inventory()` that prompts the user to enter details for items (ItemCode, ItemName, Price, Quantity) until the user enters `'N'` to stop. Store all records in `inventory.csv` with a header row `["Code", "Name", "Price", "Qty"]`.

---

### Tier 3: Logic & Edge Cases (10 Marks)

**Q5. Top Scorers CSV Filter with Type Conversion: (5 Marks)**
A CSV file `students.csv` contains rows: `Roll,Name,Marks` (e.g. `101,Mahissh,92`).  
Write a Python function `filter_top_students(min_marks)` that reads `students.csv` and creates a new CSV file `toppers.csv` containing only those students whose marks are greater than or equal to `min_marks`. Include the header row in `toppers.csv`.

**Q6. Custom Pipe Delimiter TSV Parser using DictReader: (5 Marks)**
Write a Python function `read_pipe_separated_data(filename)` that opens a pipe-delimited file (where fields are separated by `|`, e.g., `ID|Name|Role`) using `csv.DictReader`. Print each employee's `Name` and `Role` in the format: `"Name: <Name>, Role: <Role>"`.
