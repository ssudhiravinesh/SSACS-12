# Assignment 9: The CBSE Stress-Test

**Topic:** Variable Scope & Mutability (Class 12 Unit I)
**Max Marks:** 30

---

### Part 1: Predictive Analysis (10 Marks)

**Q1. Predict the output of the following code snippet. Show your dry-run steps. (4 marks)**
```python
def check(a, b):
    global x
    a = 100
    x = x + b
    b = 200
    print(a, b, x, sep="#")

x = 50
a = 10
b = 20
check(a, b)
print(a, b, x, sep="#")
```

**Q2. What will be the output of the following code? (4 marks)**
```python
def update_list(L):
    for i in range(len(L)):
        if L[i] % 2 == 0:
            L[i] = L[i] // 2
        else:
            L[i] = L[i] * 3

my_list = [10, 15, 20]
update_list(my_list)
print(my_list)
```

**Q3. Identify the scope (Local/Global) for each variable used below: (2 marks)**
```python
p = 50
def task(q):
    r = p + q
    return r
```
*   `p`: __________
*   `q`: __________
*   `r`: __________

---

### Part 2: Tracing & Shadowing (10 Marks)

**Q4. Predict the final output. Trace the value of 'val' in all three scopes. (5 marks)**
```python
val = 1
def scope_test():
    val = 10
    def nested():
        global val
        val += 1
        print("Inner:", val, end="|")
    nested()
    print("Outer:", val, end="|")

scope_test()
print("Global:", val)
```

**Q5. Explain the concept of "Object Reference" in Python using the two snippets below. Why is the original list changed in one but not the other? (5 marks)**
*Snippet A:*
```python
def f(L): L.append(5)
L1 = [1]; f(L1); print(L1)
```
*Snippet B:*
```python
def f(L): L = [1, 5]
L1 = [1]; f(L1); print(L1)
```

---

### Part 3: Practical Application (10 Marks)

**Q6. Write a function `sanitize(data_list, threshold)` that: (5 marks)**
- Takes a list of numbers.
- Replaces any number strictly greater than `threshold` with the string `"REDACTED"` **in-place** (without returning a new list).
- Demonstrate the function working by calling it and printing the list after the call.

**Q7. The Persistent Default (Challenge): (5 marks)**
Predict the output of the following three calls and explain why the second and third calls don't start with an empty list.
```python
def add_to_list(val, my_list=[]):
    my_list.append(val)
    return my_list

print(add_to_list(10))
print(add_to_list(20))
print(add_to_list(30))
```
Explanation: ____________________________________________________________________
