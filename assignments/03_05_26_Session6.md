# Assignment 6: Tuples, Dictionaries & Modules

## Part A: Quick Concepts
1.  Create a tuple named `my_tuple` with a single element: the number `100`.
2.  Explain why the following code throws an error:
    ```python
    d = {[1, 2]: "Numbers"}
    ```
3.  What is the difference between `random.randint(1, 5)` and `random.randrange(1, 5)`?

## Part B: Code Output (Dry Run)
Predict the output of the following snippets without running them. Write your trace step-by-step.

4.  **Snippet 1:**
    ```python
    t = (1, 2, 3)
    a, b, c = t
    a, b = b, a
    t = (a, b, c)
    print(t)
    ```

5.  **Snippet 2:**
    ```python
    info = {"name": "Mahissh", "subject": "CS"}
    info.update({"year": 2026, "subject": "Computer Science"})
    print(info)
    ```

6.  **Snippet 3 (Random Module):**
    ```python
    import random
    vals = [10, 20, 30]
    idx = random.randint(0, 2)
    print(vals[idx])
    ```
    What are the minimum and maximum possible values that could be printed?

## Part C: Practical Challenge
7.  **Dictionary Generator:** Create a dictionary where the keys are numbers from 1 to 5 and the values are their squares (e.g., `{1: 1, 2: 4, ...}`).
8.  **Character Count:** Write a program that takes a string (e.g., "banana") and creates a dictionary showing how many times each character appears (e.g., `{'b': 1, 'a': 3, 'n': 2}`).
