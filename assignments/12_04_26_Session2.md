# Assignment 2 — Tokens, Conditionals, Math Library

**Assigned:** 12/04/2026  
**Due:** Before Session 3 (19/04/2026)  
**Submit:** Code files + handwritten theory answers

---

## Instructions

- Complete all tiers. Don't skip Tier 3 even if it's hard — that's where you grow.
- Write theory answers in your own words. Copying = zero credit.
- Code must run. If it doesn't, comment what's broken and explain what you tried.
- Time yourself. If a question takes >30 minutes, stop and note where you're stuck. We'll discuss.

---

## Tier 1: Foundation

### Q1. Token Identification
Classify each as **Keyword**, **Identifier**, **Literal**, **Operator**, or **Punctuator**. If invalid, say why.

```
if, _count, 3.14, +, (, class, my-var, "hello", ==, 4thCol,
True, None, Price*Qty, [, and, 010, not, do, for, 2ndPlace
```

---

### Q2. Predict the Output (Don't Run First)
```python
import math

print(math.ceil(4.1))
print(math.floor(4.9))
print(math.sqrt(16))
print(math.fabs(-7.5))
print(math.pow(2, 3))
print(math.pi)
print(int(math.pi))
print(math.sin(0))
```

---

### Q3. Mutable or Immutable?
For each type, state whether it's mutable or immutable. Then write a 2-line code snippet proving it.

| Type | Mutable/Immutable? | Proof Code |
|---|---|---|
| `int` | | |
| `str` | | |
| `list` | | |
| `tuple` | | |
| `dict` | | |
| `bool` | | |

---

### Q4. Fix the Bugs
Each has **exactly one bug**. Find it, fix it, explain why.

**a)**
```python
import math
num = input("Enter a number: ")
print(f"Square root: {math.sqrt(num)}")
```

**b)**
```python
x = 10
if x > 5
    print("Greater than 5")
```

**c)**
```python
age = 15
if age >= 18:
    print("Adult")
elif age >= 13:
    print("Teenager")
else:
    print("Child")
elif age >= 65:
    print("Senior")
```

---

## Tier 2: Application

### Q5. Quadratic Equation Solver
Write a program that takes coefficients `a`, `b`, `c` of a quadratic equation (ax² + bx + c = 0) and:
1. Calculates the discriminant: `d = b² - 4ac`
2. If `d > 0`: Two real roots — print both
3. If `d == 0`: One real root — print it
4. If `d < 0`: No real roots — print "No real roots"

Use `math.sqrt()`. Handle the case where `a = 0` (not a quadratic equation).

**Formula:** `x = (-b ± √d) / 2a`

---

### Q6. Triangle Type Classifier
Write a program that takes three sides of a triangle and classifies it as:
- **Equilateral** (all sides equal)
- **Isosceles** (exactly two sides equal)
- **Scalene** (no sides equal)

But first, check if the triangle is **valid**: sum of any two sides must be greater than the third. If invalid, print "Not a valid triangle."

---

### Q7. Math Expression Evaluator
Write a program that:
1. Takes a number `x` from the user
2. Computes and prints: `sin(x)`, `cos(x)`, `e^x`, `log(x)`, `log10(x)`, `|x|`, `ceil(x)`, `floor(x)`
3. Handles the case where `x ≤ 0` (log is undefined for non-positive numbers)

---

## Tier 3: Stretch

### Q8. The Keyword Detective
Write a program that:
1. Takes a string from the user
2. Checks if it's a valid Python identifier:
   - Must start with a letter (a-z, A-Z) or underscore (_)
   - Can contain letters, digits, and underscores
   - Cannot be a Python keyword
3. Prints "Valid identifier" or "Invalid identifier — reason"

**Hint:** Use `str.isidentifier()` and the `keyword` module (`keyword.iskeyword()`).

---

### Q9. Grade Calculator with Edge Cases
Write a program that takes marks in 5 subjects (each out of 100) and:
1. Validates that each mark is between 0 and 100. If any mark is invalid, print error and stop.
2. Calculates total and percentage.
3. Assigns grade: ≥90%: A+, ≥80%: A, ≥70%: B, ≥60%: C, ≥50%: D, <50%: F
4. If the student fails in more than 2 subjects (marks < 33), the result is "FAIL" regardless of percentage.
5. Prints a formatted report card.

---

### Q10. Reverse Engineer the Condition
A program produced these outputs:

| Input (x) | Output |
|---|---|
| 5 | "Small" |
| 15 | "Medium" |
| 25 | "Large" |
| 35 | "Extra Large" |
| -5 | "Invalid" |
| 100 | "Extra Large" |

Your job: Write the `if-elif-else` chain that produces this behavior. Then write a program that asks the user to guess the boundaries and checks their guess.

---

## Theory (Handwritten)

### Q11. Short Answer
**a)** Differentiate between keywords and identifiers with two examples of each.

**b)** Why is Boolean considered a subtype of integers in Python?

**c)** What is the difference between implicit and explicit type conversion? Give one example of each.

**d)** Explain the role of indentation in Python with an example.

---

### Q12. Output Questions
**a)**
```python
import math
x = 3.7
print(math.ceil(x), math.floor(x), round(x))
```

**b)**
```python
a = 10
b = 3
if a > 5:
    if b > 5:
        print("A")
    elif a > 8:
        print("B")
else:
    print("C")
print("D")
```

**c)**
```python
x = [1, 2, 3]
y = "hello"
x[0] = 99
# y[0] = "H"  # This line is commented out
print(x)
print(y)
```

---

### Q13. Programming Question
Write a program that accepts three numbers and prints the largest among them. Use **nested `if` statements** (not `and` operator).

---

## Submission Checklist

- [ ] All Tier 1 questions answered
- [ ] All Tier 2 questions answered
- [ ] At least attempted Tier 3 questions (partial credit is fine, blank is not)
- [ ] Theory questions answered handwritten
- [ ] Code files named clearly: `q5_quadratic.py`, `q6_triangle.py`, etc.
- [ ] Time spent noted: ___ hours total
