# Session 16 Assignment: Exception Handling Part 1

**Student Name:** Mahissh  
**Subject:** CBSE Grade 12 Computer Science (Code 083)  
**Max Marks:** 30  

---

### Tier 1: Direct Application (10 Marks)

**Q1. Fill in the Blanks: (4 Marks)**
a) Errors that occur during code execution and cause program termination are called `_____________`.
b) The block of code that ALWAYS executes regardless of whether an exception occurred is `_____________`.
c) The block that executes ONLY if no exception is raised inside the `try` block is `_____________`.
d) Trying to convert a non-numeric string `"abc"` to an integer raises a `_____________Error`.

**Q2. Exception Identification: (6 Marks)**
State the specific built-in exception raised by each of the following lines of Python code:
1. `marks = [85, 90, 78]; print(marks[3])`
2. `f = open("non_existent_file.txt", "r")`
3. `val = 10 / (5 - 5)`
4. `d = {"a": 1}; print(d["b"])`
5. `"Age: " + 20`
6. `import non_existent_module`

---

### Tier 2: CBSE Exam-Style (10 Marks)

**Q3. Output Prediction: (5 Marks)**
Trace the execution and write the exact output of the following Python program:
```python
def process_data(val):
    try:
        print("Step 1")
        res = 10 / val
        print("Step 2")
    except ZeroDivisionError:
        print("Step 3: Division Error")
    except TypeError:
        print("Step 4: Type Error")
    else:
        print("Step 5: No Error")
    finally:
        print("Step 6: Cleanup")

print("--- Case A ---")
process_data(2)
print("--- Case B ---")
process_data(0)
```

**Q4. Safe Integer Input Function: (5 Marks)**
Write a Python function `get_valid_integer(prompt)` that repeatedly prompts the user for input until a valid integer is entered. Use a `try-except` block to catch `ValueError`. Return the valid integer.

---

### Tier 3: Logic & Edge Cases (10 Marks)

**Q5. Unreachable Handler Bug Fix: (5 Marks)**
Look at the following code snippet:
```python
try:
    num = int(input("Enter number: "))
    result = 100 / num
except Exception as e:
    print("General Exception caught:", e)
except ZeroDivisionError:
    print("Cannot divide by zero!")
except ValueError:
    print("Invalid integer input!")
```
1. Explain why `except ZeroDivisionError:` and `except ValueError:` will NEVER be executed.
2. Rewrite the code with the handlers arranged in the correct order.

**Q6. Return-in-Finally Edge Case Tracing: (5 Marks)**
Predict the output of the following function call and explain the rule governing return values inside `finally`:
```python
def test_returns():
    try:
        print("A")
        return 10
    except:
        print("B")
        return 20
    finally:
        print("C")
        return 30

result = test_returns()
print("D:", result)
```
