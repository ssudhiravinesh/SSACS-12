# Assignment 4: Strings — The Data Weaver

**Topic:** String Manipulation, Slicing, and Methods  
**Total Marks:** 20  
**Deadline:** 29/04/2026  

---

## Part 1: Syntax & Output (6 Marks)

**Q1. Trace the Output**
What is the output of the following code snippets?
a) 
```python
s = "Welcome to CBSE"
print(s[3:10:2])
```
b)
```python
s = "Python"
print(s[-1:-5:-2])
```

**Q2. Correction Challenge**
Correct the following code so it successfully changes "Rain" to "Main" without using the `replace()` method:
```python
word = "Rain"
word[0] = "M"
print(word)
```

**Q3. One-Liners**
Write a single line of Python code for each:
- Convert `"Maverick"` to all lowercase and check if it ends with `"k"`.
- Remove the prefix `"Session_"` from the string `"Session_04"`.

---

## Part 2: Logical Programs (8 Marks)

**Q4. Domain Extractor**
Given an email string like `"student@school.edu"`, write a program that extracts only the domain part (`"school.edu"`) WITHOUT using the `.split()` method.

**Q5. Toggle Case**
Write a program that takes a string and converts all uppercase characters to lowercase and vice versa (e.g., `"PyThOn"` becomes `"pYtHoN"`). 
*Constraint: Do not use the `.swapcase()` method.*

---

## Part 3: The Intuition Challenge (4 Marks)

**Q6. Character Frequency**
Write a program to count the occurrences of a specific character in a string WITHOUT using the `.count()` method.
Example: If string is `"banana"` and char is `"a"`, output should be 3.

---

## Part 4: Board Exam Prep (2 Marks)

**Q7. Theory Check**
Explain the difference between `S.find()` and `S.index()` with a suitable example. What happens if the search term is not found?
