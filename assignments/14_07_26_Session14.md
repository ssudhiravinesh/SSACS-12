# Session 14 Assignment: Binary Files CRUD Operations

**Student Name:** Mahissh  
**Subject:** CBSE Grade 12 Computer Science  
**Max Marks:** 30  

---

### Tier 1: Direct Application (10 Marks)

**Q1. Fill in the Blanks: (4 Marks)**
a) The module used to handle file deletions and renaming in Python is `_____________`.
b) To delete a file named `old.dat`, we call `os._____________("old.dat")`.
c) To rename `temp.dat` to `main.dat`, we call `os._____________("temp.dat", "main.dat")`.
d) If a file is currently open inside a context manager, trying to delete or rename it will raise a `_____________Error` on Windows systems.

**Q2. OS Commands Sequence Bug: (6 Marks)**
Look at the following segment of a record deletion function:
```python
# Segment: File replacement
# Assume fin and fout context blocks have ended.
os.rename("temp.dat", "student.dat")
os.remove("student.dat")
```
1. Explain the bug in this code. Why will it result in fatal data loss?
2. Write the corrected sequence of the two commands.

---

### Tier 2: CBSE Exam-Style (10 Marks)

**Q3. Record Update Function: (5 Marks)**
Write a Python function `update_salary(emp_id, bonus)` that reads records from `employee.dat` (each record is a dictionary: `{"id": 101, "name": "Raj", "salary": 50000}`). The function should increase the salary of the employee whose ID matches `emp_id` by adding `bonus` to it. Overwrite the file with the changes using the **temporary file method**.

**Q4. Record Deletion Function: (5 Marks)**
Write a Python function `delete_employee(emp_id)` that removes the record of the employee whose ID matches `emp_id` from `employee.dat` using a **temporary file**. Print a success message if the record was deleted, or `"Employee not found"` if the record was not present in the file.

---

### Tier 3: Logic & Edge Cases (10 Marks)

**Q5. Grace Marks Incrementor (In-Memory Method): (5 Marks)**
Write a Python function `grant_grace_marks(grace_amt)` that reads records from `student.dat` (`{"roll": 1, "name": "Mahissh", "marks": 88}`). If a student's marks are between `30` and `34` (inclusive), add `grace_amt` to their marks. Overwrite the file with the modified records using the **in-memory buffer method** (reading all records into a list first).

**Q6. Traversal Seek Overwrite Trap: (5 Marks)**
Explain two reasons why attempting to update a binary file in place using `f.seek()` and `pickle.dump()` in `"rb+"` mode is bad programming practice and frequently results in corrupted data files.
