# Assignment 10: Using Python Libraries

**Topic:** Math, Random, and Statistics.
**Max Marks:** 30

---

### Part 1: Module Basics (10 Marks)

**Q1. Predict the results of the following code: (4 marks)**
a) `math.ceil(-5.8)`
b) `math.floor(-5.8)`
c) `math.fabs(-10.2)`
d) `math.pow(4, 0.5)`

**Q2. What is the difference between `randint(1, 5)` and `randrange(1, 5)`? List all possible integers returned by each. (4 marks)**

**Q3. Rewrite the code to fix the error: (2 marks)**
```python
import math
print(pi * 5 * 5)
```

---

### Part 2: Board-Style Tracing (10 Marks)

**Q4. [Predict Output] A board exam favorite. (5 marks)**
What are the minimum and maximum possible values for `MY_RAND`? Also, list two possible outputs for the code below:
```python
import random
CITIES = ["Delhi", "Mumbai", "Chennai", "Kolkata"]
MY_RAND = random.randint(0, 2)
for i in range(MY_RAND, 3):
    print(CITIES[i], end=":")
```
- Min MY_RAND: __________ Max MY_RAND: __________
- Output 1: ________________________________________
- Output 2: ________________________________________

**Q5. [Choice Logic] (5 marks)**
If `random.random()` returns `0.6`, what will be the result of the following expression?
`int(random.random() * 50) + 10`
*Show your step-by-step calculation.*

---

### Part 3: Coding & Theory (10 Marks)

**Q6. Statistics Application: (5 marks)**
Write a function `analyze_marks(marks_list)` that prints the Mean, Median, and Mode of the student marks. Ensure you import the necessary module.

**Q7. The Namespace Question: (5 marks)**
Explain why using `from math import *` is generally discouraged in large professional projects. What happens if two different modules have a function with the same name?
