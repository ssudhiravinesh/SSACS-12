# Session 11 Assignment: File Handling — Part 1 (Text Files)

**Student Name:** Mahissh  
**Subject:** CBSE Grade 12 Computer Science  
**Max Marks:** 30  

---

### Tier 1: Direct Application (10 Marks)

**Q1. File Modes Identification (4 Marks)**
Specify the correct file opening mode (`'r'`, `'w'`, `'a'`, `'r+'`) for each scenario:
a) To append new records to an existing log file without deleting existing data: `mode = '____'`
b) To read from a file that MUST already exist on disk: `mode = '____'`
c) To write output to a file, creating a new file or completely overwriting an existing one: `mode = '____'`
d) To open an existing text file for both reading and writing starting from character 0: `mode = '____'`

**Q2. Method Return Types (3 Marks)**
State the exact Python return data type (`str`, `list`, `int`, etc.) for each of the following function calls:
a) `file_obj.read(15)` $\rightarrow$ ____________
b) `file_obj.readline()` $\rightarrow$ ____________
c) `file_obj.readlines()` $\rightarrow$ ____________

**Q3. Simple Code Writing — Line Counter (3 Marks)**
Write a Python function `count_lines(filename)` that accepts a text file name and returns the total number of lines in that file. Use the `with` statement.

---

### Tier 2: CBSE Exam-Style & Edge Cases (10 Marks)

**Q4. Predict Output — Pointer & Newline Trap (5 Marks)**
Given a file `sports.txt` containing the following text:
```text
Cricket
Football
Tennis
```
Predict the exact output of the following Python code snippet:
```python
with open("sports.txt", "r") as f:
    line1 = f.readline()
    line2 = f.read(4)
    print(repr(line1))
    print(repr(line2))
    rest = f.readlines()
    print(rest)
```

**Q5. Vowel & Consonant Counter Function (5 Marks)**
Write a function `vowel_count(filename)` that reads a text file and counts how many vowels (`a, e, i, o, u`, case-insensitive) and consonants are present in the file. Ignore spaces, numbers, and punctuation marks.

---

### Tier 3: Logic & Memory Optimization (10 Marks)

**Q6. Filtering & Copying Text (5 Marks)**
Write a Python function `filter_comments(infile, outfile)` that reads an input code file (`infile`) and writes all lines that do NOT start with the `#` character into an output file (`outfile`).

**Q7. The Memory Optimization Challenge (5 Marks)**
A system administrator has a large 5 GB server log file `server.log`. They want to count how many lines contain the keyword `"ERROR"`.
1. Explain why using `lines = f.readlines()` is bad programming practice for large files.
2. Write a memory-efficient Python function `count_errors(filename)` that counts the `"ERROR"` lines without crashing system RAM.
