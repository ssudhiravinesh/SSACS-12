# Session 13 Assignment: Binary Files & Pickling

**Student Name:** Mahissh  
**Subject:** CBSE Grade 12 Computer Science  
**Max Marks:** 30  

---

### Tier 1: Direct Application (10 Marks)

**Q1. Fill in the Blanks: (4 Marks)**
a) The process of converting a Python object hierarchy into a byte stream is called ______________.
b) The function used to write an object to a binary file is `pickle._____________()`.
c) The binary file mode used to append records to a file without overwriting existing contents is `'____'`.
d) The function used to read an object back from a binary file is `pickle._____________()`.

**Q2. Simple Pickling Program: (6 Marks)**
Write a Python program that:
1. Imports the correct module required for binary serialization.
2. Defines a dictionary: `employee = {"id": 101, "name": "Raj", "salary": 50000}`.
3. Opens a file named `emp.dat` in write-binary mode using the `with` statement.
4. Serializes the dictionary into `emp.dat`.

---

### Tier 2: CBSE Exam-Style (10 Marks)

**Q3. Traversal & Search Function: (5 Marks)**
Write a Python function `search_employee(emp_id)` that reads records from `emp.dat`. Each record is a dictionary containing `"id"`, `"name"`, and `"salary"`. The function should find and print the name of the employee whose ID matches `emp_id`. If the employee is found, print their name; if the file contains no matching records, print `"Employee not found"`. (Handle the `EOFError` loop correctly).

**Q4. Mode Analysis: (5 Marks)**
Explain the difference in behavior between opening a binary file in `'wb'` mode versus `'ab'` mode. Focus your answer on:
1. The initial position of the file pointer.
2. What happens to existing content inside the file.

---

### Tier 3: Logic & Data Aggregation (10 Marks)

**Q5. Student Grade Aggregator: (5 Marks)**
A binary file `grades.dat` contains multiple student detail dictionaries. Each dictionary looks like this: `{"roll": 12, "name": "Amit", "marks": 85}`.
Write a Python function `count_toppers()` that reads this file, counts how many students scored marks greater than or equal to `90`, and prints the total count.

**Q6. Empty File Edge Case: (5 Marks)**
What error / exception occurs when you attempt to run `pickle.load()` on a binary file that exists but is completely empty (0 bytes)?
Write a small code snippet demonstrating how you can load from a binary file safely without letting the program crash if the file is empty.
