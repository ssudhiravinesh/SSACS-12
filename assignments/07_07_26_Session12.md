# Session 12 Assignment: Writing, Pointers, and Text Filtering

**Student Name:** Mahissh  
**Subject:** CBSE Grade 12 Computer Science  
**Max Marks:** 30  

---

### Tier 1: Direct Application (10 Marks)

**Q1. Pointer Arithmetic & Dry-Run (5 Marks)**
Given a text file named `data.txt` containing the following text:
```text
Python123
```
Predict the output of the following Python program:
```python
with open("data.txt", "r") as f:
    print(f.read(3))
    print(f.tell())
    f.seek(6)
    print(f.read(3))
    f.seek(0)
    print(f.read(1))
```

**Q2. Write vs Writelines Syntax (5 Marks)**
Look at the following code snippet:
```python
lines = ["First Line\n", "Second Line\n"]
with open("output.txt", "w") as f:
    f.write(lines)
```
1. Explain why this code crashes with a `TypeError`.
2. Rewrite the code using the correct writing function to save the list to disk.

---

### Tier 2: CBSE Exam-Style (10 Marks)

**Q3. Counting Words Starting with "A" / "a" (5 Marks)**
Write a Python function `count_A_words(filename)` that reads a text file and counts how many words start with the letter `'A'` or `'a'`.
*Example input:* `"An apple a day keeps the doctor away."` $\rightarrow$ should return `4`.

**Q4. Copying and Capitalizing Lines (5 Marks)**
Write a Python function `copy_capitalized(infile, outfile)` that reads the file `infile` line-by-line. If a line starts with a vowel (A, E, I, O, U - case-insensitive), convert the entire line to uppercase and write it to `outfile`. Otherwise, write the line as-is.

---

### Tier 3: Logic & Edge Cases (10 Marks)

**Q5. Removing Lines Containing "delete" (5 Marks)**
Write a Python function `remove_delete_lines(filename)` that filters out any line containing the word `"delete"` (case-insensitive) from the file. The filtered lines must overwrite the original file (modify the file in-place).

**Q6. Python 3 Seek Trap Explanation (5 Marks)**
A student attempts to print the last 5 bytes of a text file using this code:
```python
with open("log.txt", "r") as f:
    f.seek(-5, 2)
    print(f.read())
```
This program throws an error.
1. What is the name of the error / exception raised?
2. Why is this seek operation invalid in Python 3 text mode?
3. How can we fix this program to work correctly? (Specify the code change).
